import { UserFreezeInterceptor } from './user-freeze.interceptor';

describe('UserFreezeInterceptor', () => {
  it('should be defined', () => {
    expect(new UserFreezeInterceptor()).toBeDefined();
  });
});
